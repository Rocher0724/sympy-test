import { describe, it } from "vitest";

describe("T01: Pyodide 로딩 및 SymPy 패키지 설치 확인", () => {
  it.todo("should load Pyodide successfully (browser test)");
  it.todo("should install and import sympy (browser test)");
  it.todo("should have parse_latex available (browser test)");
});

describe("T02: parse_latex 함수로 간단한 LaTeX 파싱 확인", () => {
  it.todo("should parse 'x+1' to SymPy expression (browser test)");
  it.todo("should parse fraction '\\\\frac{1}{2}' correctly (browser test)");
});
